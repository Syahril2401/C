#include <stdio.h>

int main() {
	
	int a;
	int b;
	char c;
	
	
	scanf("%d %c %d =", &a, &c, &b);
    if (c == '+') {
        printf("%d\n", a + b);
    }
    
	scanf("%d %c %d =", &a, &c, &b);
    if (c == '+') {
        printf("%d\n", a + b);
    }
    
	scanf("%d %c %d =", &a, &c, &b);
    if (c == '+') {
        printf("%d\n", a + b);
    }

	
	
return 0;	
}
